export default function Badge({ children, variant='default', className='' }){ const v={default:'bg-primary/50 text-secondary',success:'bg-primary/40 text-secondary',warning:'bg-primary/60 text-secondary',danger:'bg-secondary/10 text-secondary',info:'bg-primary/70 text-secondary'}; return <span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-semibold ${v[variant]} ${className}`}>{children}</span>}
